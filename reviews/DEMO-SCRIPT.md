# 심사 시연 대본 — 3분

라이브: https://scam-radar-sooty.vercel.app
사전 확인(시연 30초 전): `curl -s -o /dev/null -w "%{http_code}" https://scam-radar-sooty.vercel.app/api/stats` → `200` 이면 A 경로, `500`/`503` 이면 B 경로.
(2026-09-20 22:52 KST 확인 시점엔 500 — DB 경로 전부 불통, `/analyze` 만 정상.)

시작 전 한 문장 (공통):
> "시연에 나오는 계정은 전부 합성 데이터입니다 — `sample_`·`syn_` 로 시작하는 핸들은 실존 계정이 아니고, 실제로 수집한 데이터는 공개 리포트에서 핸들을 가렸습니다."

---

## A 경로 — DB 연결됨 (약 3분)

| # | 시간 | 화면·조작 | 말할 것 |
|---|---|---|---|
| 1 | 0:00 | `/` 입력창에 `sample_vipstock77` → 조회 | "핸들 하나를 넣으면 이 계정이 남긴 글에서 어떤 신호가 몇 개 있는지 점수와 개수로만 보여줍니다 — '의심'·'주의' 같은 판단 단어는 화면에 없습니다." |
| 2 | 0:25 | 배지 아래 "감지된 신호와 설명" — 첫 3개는 펼쳐져 있음, 4번째 "자세히 보기" 클릭 | "근거마다 무엇이 잡혔고, 왜 신호인지, 실제 유인글 예시, 그리고 정상 글에서는 어떻게 다른지 네 단락으로 풀어 씁니다. 판단은 읽는 사람 몫입니다." |
| 3 | 0:55 | 아래 "연락처" 칩(빨간 `텔레그램 vip_stock_room77`) → "같은 연락처를 쓰는 계정" 목록 | "문구는 바꿔도 연락처는 못 바꿉니다. 같은 텔레그램 방을 안내하는 계정을 묶어 한 운영자 단위로 봅니다." |
| 4 | 1:15 | 상단 메뉴 `캠페인` → `/clusters` | "같은 본문이 서로 다른 계정 몇 개에서 나오는지 — 일회용 계정에 살포하는 형을 잡는 축입니다. 3계정 이상이면 연락채널 없이도 강한 신호로 올라갑니다." |
| 5 | 1:35 | 홈으로 → "리포트 보기" 또는 직접 `/report/threads:sample_vipstock77` | "계정·일시·내용 요약·연락처·증거 캡처·근거 설명·신고처를 한 장으로. 이건 초안이고, 제출은 사람이 합니다. 오탐으로 닫힌 계정이면 맨 위에 '신고 근거로 쓰지 말라'는 배너가 뜹니다." |
| 6 | 2:00 | 메뉴 `텍스트 분석` → `/analyze`, 텍스트창에 붙여넣기: `대신증권 재직중입니다. 사원증 인증 사진 올려요. 종목 정보 필요하신 분 댓글 남겨주세요, 제가 직접 상담해 드릴게요` (샘플 4 버튼과 동일) | "텔레그램이 한 글자도 없는데 83점 강한 신호입니다 — 실존 기관명 + 재직·사원증 주장 + 정보를 주겠다는 제안, 이 세 조각의 결합이 사칭 구조입니다. 오른쪽 '같은 문구 쓴 계정 수'에 3을 넣으면 100까지 갑니다. 브라우저에서만 계산하고 서버로 안 보냅니다." |
| 7 | 2:30 | 메뉴 `검토` → `/review` (ADMIN_TOKEN 로그인) → HIGH 미검토 탭 → `sample_vipstock77` 행 → `사기 확인` 또는 `오탐` 클릭 | "여기서 처음으로 '사기'라는 단어가 나옵니다. 사람이 누른 판단만 그 단어를 쓰고, 기계 점수는 지우지 않고 밑에 남겨 나중에 가중치 보정에 씁니다." |
| 8 | 2:50 | 터미널 한 줄 | "금융기관·플랫폼 봇은 이 API 하나로 붙습니다." |

8번 curl:
```bash
curl -s https://scam-radar-sooty.vercel.app/api/accounts/sample_vipstock77 | jq '{score:.account.score, status:.effectiveStatus, signals:(.account.reasons|length), contacts:[.entities[].value], linked:[.sharedWith[].handle]}'
```

---

## B 경로 — DB 불통 (`/analyze` 만, 약 2분 30초)

시작 문장 뒤에 한 마디 추가:
> "조회 DB 가 지금 연결이 안 돼 있어 텍스트 분석 화면으로 엔진을 보여 드리겠습니다. 판정 엔진은 순수 TypeScript 라 브라우저에서 그대로 돕니다."

| # | 화면·조작 | 말할 것 |
|---|---|---|
| 1 | `/analyze` → `샘플 1` 클릭 (급등주 + ㅌㄹㄱㄹ @stock_king77) | "초성 'ㅌㄹㄱㄹ'을 텔레그램으로 되돌린 뒤 매칭합니다. 아래 정규화 결과에 변형 기법과 변형률이 찍힙니다 — 정상 사용자는 자기 연락처를 난독화하지 않으니 변형 자체가 신호입니다." |
| 2 | 근거 목록 "자세히 보기" 하나 펼치기 | "각 근거는 무엇이/왜/실제 예시/정상 글 네 단락. 화면 어디에도 '의심'·'주의' 가 없고 점수와 신호 개수만 있습니다." |
| 3 | `샘플 4` 클릭 (대신증권 재직 + 사원증 + 종목 정보 드릴게요) | "텔레그램 없이 83점. 기관명 3점 + 재직·사원증 17점 + 종목 어휘 18점 + 사칭 결합 25점 + 사칭·투자 동반 10점 + 행동 유도 10점. 결합이 없으면 — 예를 들어 '삼성전자 재직 중인데 구내식당' — 12점입니다." |
| 4 | 같은 화면, `샘플 5` 클릭 (삼성전자 좀 올랐네요) → 다시 샘플 4 로 돌아와 "같은 문구 쓴 계정 수" 에 `3` 입력 | "기관명 단독은 3점, 정상 글입니다. 반대로 같은 문구가 3개 계정에서 나오면 +25 — 일회용 계정 살포 축입니다. 연락채널·사칭·살포 셋 중 하나는 있어야 강한 신호가 됩니다." |
| 5 | 텍스트창에 `리딩방 사기 당한 후기. 텔레그램 무료방 들어갔다가 300만원 날렸어요. 조심하세요` 붙여넣기 | "유인 어휘가 다 있어도 피해·경고 맥락이면 상한을 걸어 강한 신호를 포기합니다. 피해자를 다시 지목하지 않기 위해서입니다." |
| 6 | `/review` 열기 (로그인 화면까지만) | "최종 판단은 이 화면에서 사람이 합니다. '사기'라는 단어는 사람이 누른 뒤에만 나옵니다." |
| 7 | 터미널 한 줄 | "봇·금융기관용 무상태 API 도 DB 없이 돕니다." |

7번 curl (DB 불필요):
```bash
curl -s -XPOST https://scam-radar-sooty.vercel.app/api/analyze -H 'content-type: application/json' \
  -d '{"text":"대신증권 재직중입니다. 사원증 인증 사진 올려요. 종목 정보 필요하신 분 댓글 남겨주세요, 제가 직접 상담해 드릴게요","account":{"clusterAccounts":3}}' \
  | jq '{score, label, signals:(.reasons|length), top:[.reasons[:3][]|"\(.points) \(.label)"]}'
```
기대 출력: `score: 100, label: "HIGH"`, 상위 근거에 "사칭 구조" 와 "서로 다른 계정 여러 개가 같은 문구 게시".

---

## 질문 대비 한 줄 답

- "실데이터로 잡혔나?" → "첫 실수집 250건에서 사칭·살포형은 0건 — 검색 시점엔 이미 삭제돼 있었다. 피드·댓글 스트림 주기 수집이 다음 단계. 오탐(타로 '리딩')·미탐(경계 점수 유인글)은 리포트에 그대로 적었다."
- "왜 LLM 을 안 쓰나?" → "40~69 경계 구간과 요약 초안에만. 판정 로직이 코드에 있어야 재현·검증·보정이 된다."
- "명예훼손은?" → "기계는 지표만, 판단 단어는 사람이 누른 뒤에만. 리포트는 초안이고 제출은 사람. 실계정은 공개 리포트에서 가렸다."
