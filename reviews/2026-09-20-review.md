# 계획 대 구현 감사 — 2026-09-20 22:50 KST

독립 리뷰. 코드는 수정하지 않았다. 기준은 소현의 계획 문장 그대로:
"해외에서 만든 계정이 한국인 행세 → 대신증권·삼성전자 재직 사칭(위조 사원증·급여명세 사진) → 같은 문구를 일회용 계정 다수에 살포. 텔레그램 유인은 부차. UI 는 판단 단어 없이 지표(점수·신호 수)만, 근거는 예시와 함께 길게. 최종 판단·신고는 사람. 금융기관은 API 로."

태그: [OK] 계획대로 동작 · [GAP] 계획과 구현이 다름 · [RISK] 동작하지만 제출·시연·법적 위험

---

## 0. 라이브 사이트 상태 (제출 전 최우선)

- [RISK] **DB 경로 전부 500.** 22:52 KST 기준 `/api/stats` `/api/accounts/sample_vipstock77` `/api/clusters` `/api/entities/…` `/report/threads:sample_vipstock77` `/clusters` 모두 HTTP 500. `/` `/analyze` `/review` 는 200, `POST /api/analyze` 는 정상(83 HIGH). `ADMIN_TOKEN` 은 설정돼 있음(오답 401). → `POSTGRES_URL` 은 주입돼 있어 `hasDb=true` 인데 쿼리가 던진다 — 스키마 미푸시(`npm run db:push`) 또는 URL 만료가 유력. `src/db/index.ts:9` 는 URL 존재만 보고 `hasDb` 를 정하므로 홈 화면(`src/app/page.tsx:15` catch → `stats=null`, `page.tsx:41` 배너는 `!hasDb` 에서만)에 아무 경고도 안 뜬다. 심사위원이 조회하면 조용히 500.
- [OK] `/analyze` 는 순수 클라이언트 계산이라 DB 와 무관하게 시연 가능 (`src/app/analyze/page.tsx:21`). DEMO-SCRIPT 에 fallback 경로 포함.

## (a) score.ts 추적 — 사칭 + 살포가 HIGH 에 닿는가

- [OK] **사칭형은 텔레그램·살포 없이 단독으로 HIGH.** 예시 게시물(= `/analyze` 샘플 4, `src/app/analyze/page.tsx:11`):
  "대신증권 재직중입니다. 사원증 인증 사진 올려요. 요즘 장 흐름이랑 종목 정보 필요하신 분 댓글 남겨주세요, 제가 직접 상담해 드릴게요"

  | 단계 | 근거 | 계산 | 누적 |
  |---|---|---|---|
  | 어휘 impersonate (`score.ts:123-133`, 가중 `CAT_WEIGHT.impersonate=10`) | 대신증권 0.3 → 3, 재직중 0.9 → 9, 사원증 1.0 → 10 | 22 → 상한 20 (`score.ts:58,161`) | 20 |
  | 어휘 invest (가중 14) | 종목정보 0.9 → 12.6, 종목 0.4 → 5.6 | 18.2 | 38.2 |
  | contact = 0 | `combo:contact+invest` 없음 (`score.ts:175`) | 0 | 38.2 |
  | 사칭 결합 (`score.ts:187-196`) | sawInstitution ∧ sawCredential ∧ offered(invest>0) | +25 | 63.2 |
  | 사칭+투자 (`score.ts:197-200`) | invest>0 | +10 | 73.2 |
  | CTA (`score.ts:211-222`) | contact=0 이지만 impersonateCombo → "드릴게요" 0.8×6=4.8 + "주세요" 4.8 = 9.6 | +10 | 83.2 |
  | 게이트 (`score.ts:301`) | impersonateCombo 로 열림 | — | **83 HIGH** |

  실측: 로컬 `scoreText` 와 라이브 `POST /api/analyze` 모두 83 HIGH.
- [OK] 살포 가점: `clusterAccounts=3` → `spreadPoints` +25 (`score.ts:74-79,263-266`) → 108 → clamp 100. `clusterAccounts=5` → +32. 게이트 ③ 은 3계정부터 (`score.ts:68,301`).
- [OK] 살포 단독 분리: 무해 문구 + 5계정 → 35 LOW (spread 32 + 삼성전자 3). 투자 어휘만 + 3계정 → 74 HIGH (spread 25 + 어휘 49; 연락채널 없이도 게이트 ③ 으로 열림). 계획("같은 문구 다중 계정 살포 자체가 신호")과 일치.
- [OK] 하드 네거티브: "삼성전자 재직 중인데 구내식당 맛없다" 12 LOW / "대신증권 다니는 분 알려주세요" 3 LOW / "대신증권 재직중 급여명세서 올립니다"(제안 없음) 20 LOW.
- [GAP] **파이프라인에서 살포 가점은 마지막에 들어온 계정에만 붙는다.** `src/lib/pipeline.ts:252-257` 는 새 게시물의 `clusterAccounts` 만 계산하고, 먼저 수집된 클러스터 구성원의 점수는 재계산하지 않는다(연락처 공유는 `recomputeSharedContact` 로 재계산함, `pipeline.ts:345-356`). 계정 5개가 순서대로 들어오면 1번 계정은 spread 0, 5번만 +32. RESULTS.md 에 "다음 단계"로 적혀 있으나 이것이 계획의 핵심 신호다.
- [GAP] **'해외에서 만든 계정' 신호가 엔진에 없다.** `IngestSchema` (`pipeline.ts:20-31`) 에 국가·언어·가입지 필드가 없고 `score.ts` 어디에도 없다. `explain.ts:82,165` 는 "실제로는 해외에서 만든 계정" 이라고 설명하지만 코드는 그것을 보지 않는다 — 설명이 코드보다 앞서 있다.
- [GAP] **사원증·급여명세 '사진'은 못 본다.** 텍스트 어휘("사원증", "급여명세")만 매칭(`lexicon.ts:239-240`). 이미지에만 증빙이 있는 글은 0점. 첫 실수집에서 본문 없는 13건 제외됨(`reports/2026-09-20-first-collection.md`). OCR·비전 없음.
- [GAP] **LLM 재판정 프롬프트가 구(舊) 판정축(연락채널) 그대로.** `src/lib/llm.ts:48`: "(a) 텔레그램·카톡 등 외부 메신저로 유도하면서 (b) 투자 수익을 미끼로" — 사칭형 REVIEW(40~69) 게시물은 메신저 유도가 없으므로 LLM 이 benign 으로 답해 -20 (`pipeline.ts:276`) 될 수 있다. 재설계에서 이 파일만 안 바뀌었다.
- [RISK] 라벨 불일치: `score.ts:219` CTA 라벨이 "연락채널 + 행동 유도 문구" 인데 contact=0 인 사칭 경로에서도 붙는다(위 예시가 그 경우). 화면에 "연락채널" 이 없는 글에 "연락채널 +…" 이 뜬다.
- [RISK] 계정 단위 `cluster` 가점이 두 군데서 다르게 계산된다: `score.ts:257-260` 는 ≥3건·상한 20, `pipeline.ts:337` 는 ≥2건·상한 15. 계정 근거 목록의 점수 합이 배지 점수와 안 맞는 원인 중 하나(계정 점수 = 최고 게시물 점수 + shared-contact, `pipeline.ts:194,330`).
- [RISK] `급여명세서` 한 단어가 `급여명세`(10) + `명세서`(7) 두 항목으로 이중 매칭(`lexicon.ts:240-241`, 라벨이 달라 `seenLabel` 을 통과). 상한 20 에 걸려 총점 영향은 작지만 근거 목록에 같은 증거가 두 줄.
- [RISK] 평가셋은 게시물 1건 단위라 살포가 측정되지 않는다(`eval/RESULTS.md` "남은 약점"). precision 1.000 은 사칭+어휘 축만의 수치. 첫 실수집 250건에서 사칭·살포형 0건 — 실데이터로 이 축이 검증된 적 없음.

## (b) UI 판단 어휘 — 잔존 grep

요청된 패턴 (`grep -rnE '의심|주의|사기 가능성|사람 확인|검토 필요' src/`):

```
src/lib/labels.ts:6: * 기계 라벨의 표시 문구는 **지표(indicator)만** 말한다 — "신호가 얼마나 강한가". "사기", "의심", "주의"
src/engine/confusables.ts:4: * 순서 주의: 이 테이블은 NFD 직후, NFKC 이전에 적용된다 (UTS #39 skeleton 순서).
src/engine/explain.ts:4: * 화면은 "사기다/의심된다" 라고 말하지 않는다. 대신 **무엇이 감지됐고, 그것이 왜 신호가 되며,
src/engine/lexicon.ts:7:/** impersonate 범주의 하위 구분 — 기관명 vs 재직·증빙 주장. 결합 규칙(score.ts)이 이 둘을 구분한다 */
src/engine/lexicon.ts:275:  { term: "주의하세요", label: "경고 문구", w: 1.2, strong: true },
src/engine/lexicon.ts:276:  { term: "주의하시", label: "경고 문구", w: 1.0, strong: true },
src/engine/lexicon.ts:277:  { term: "의심", label: "의심 권유", w: 0.6 },
src/engine/engine.test.ts:241:  it("UI 설명 문구에 판단 어휘(사기 가능성/의심/주의)를 쓰지 않는다 — 수법 설명 문맥 제외", async () => {
src/engine/engine.test.ts:243:    for (const k of ["HIGH", "REVIEW", "LOW", "UNKNOWN"]) expect(STATUS_TEXT[k]).not.toMatch(/사기|의심|주의|검토 필요/);
```

- [OK] 위 히트는 전부 주석·사전(COUNTER_TERMS 는 *감점* 어휘)·테스트. 화면에 렌더되는 문자열에는 요청 패턴이 0건. `STATUS_TEXT` (`labels.ts:20-27`) 는 강한 신호/신호 감지/신호 미약/수집 기록 없음 — 계획대로.
- [OK] 판단 어휘는 사람 판단 결과에만: `labels.ts:21` "검토 완료 · 사기 확인", `HumanVerdict.tsx:15` "사기 계정으로 확인됨", `review/page.tsx:186` "사기 확인" 버튼(검토자 화면). 계획의 "사람이 누르기 전에는 사기라는 단어가 안 나온다" 의 예외로 정당.

확장 grep (`사기|범죄|피신고|신고용|가능성이 (큽|있)` — 렌더 문자열만):

```
src/app/layout.tsx:6:  title: "리딩방 레이더 — SNS 투자사기 유인 계정 탐지",
src/app/page.tsx:57:  …계정명·일시·범죄사실요약·증거 캡처…
src/app/clusters/page.tsx:17:  …같은 문구를 여러 계정이 뿌리면 조직적 캠페인일 가능성이 큽니다.
src/app/report/[id]/page.tsx:49:  <h2>1. 피신고 계정</h2>
src/app/report/[id]/page.tsx:69:  <h2>3. 내용 (범죄사실 요약 — 요건사실 위주)</h2>
src/components/Lookup.tsx:88:  신고용 리포트 보기
src/components/Lookup.tsx:131:  같은 연락처를 안내하는 계정들은 같은 운영자·조직에서 관리할 가능성이 있습니다.
src/lib/llm.ts:113-122 templateSummary: "1) 피신고 계정: …" (사람 검토 전 계정에도 생성)
```

- [GAP] **브라우저 탭 제목이 모든 페이지에서 "SNS 투자사기 유인 계정 탐지"** (`layout.tsx:6`). SUBMISSION.md 5항("사람이 '사기 확인'을 누르기 전에는 사기라는 단어가 나오지 않는다")과 정면 충돌. 심사위원이 첫 화면에서 본다.
- [GAP] **리포트가 LOW/REVIEW 계정에도 "피신고 계정" · "범죄사실 요약" 제목을 단다.** `buildReport` (`src/lib/report.ts:45-98`) 에 라벨·사람판단 게이트가 없고, `report/[id]/page.tsx:49,69` 제목은 상태 무관 고정. 기계가 12점 준 계정을 조회해도 "1. 피신고 계정" 이 뜬다.
- [RISK] `clusters/page.tsx:17` "조직적 캠페인일 가능성이 큽니다", `Lookup.tsx:131` "같은 운영자·조직 … 가능성" — 지표가 아니라 추론 문장. 홈 카드 `page.tsx:57` "범죄사실요약" 도 같은 결.
- [RISK] `ScoreBadge.tsx:6-8` 색이 HIGH=빨강 / LOW=초록. 단어는 지표지만 색은 판단을 말한다. 계획 위반은 아니나 "판단은 보는 분이" 와 어긋나는 시각 신호.

## (c) explain.ts — 빈 항목·제너릭·코드와 불일치

- [OK] 빈 값은 없다. `explainReason` (`explain.ts:365-398`) 은 어떤 코드도 4필드를 채우고, `PATTERNS` 7종은 `PATTERN_TEXT` (`explain.ts:317-360`) 에 전부 있다. 테스트 `engine.test.ts:222-240` 가 길이 >5 를 검사.
- [GAP] **`veto:타로·점술 맥락 (투자 리딩 아님)` 은 전용 설명이 없어 제너릭 veto 문구로 떨어진다** (`explain.ts:373-380`: "합법 광고·기사·공공 캠페인·풍자·해명처럼 보이는 형식" — 타로와 무관). `lexicon.ts:346-350` 에 오늘 추가된 veto 인데 설명 표는 갱신 안 됨. 첫 실수집 오탐 2건이 바로 이 케이스라 시연 중 만날 확률이 높다.
- [GAP] 최종 fallback (`explain.ts:392-397`) 의 `example: "-"`, `why: "이 항목은 점수 계산에 반영된 신호입니다"` — 제너릭. 현재 코드 경로로는 도달하지 않지만 새 코드를 추가하면 바로 이게 뜬다.
- [GAP] `term:회사에서`·`팀장`·`부장`·`과장`·`매니저` (`lexicon.ts:233-237`, sub=credential) → `CREDENTIAL` 설명 (`explain.ts:114-119`) 으로 라우팅되는데, 그 설명은 "사원증·급여명세서·재직증명서를 보여 주겠다거나" 로 시작한다. "과장" 한 단어에 사원증 설명이 붙는다.
- [GAP] `combo:contact+invest` benign (`explain.ts:155`): "결합이 없는 글 — 수익 자랑만 있고 오라는 말이 없는 글 — 은 아무리 단어가 많아도 강한 신호로 분류하지 않습니다" — 재설계 후 거짓. 사칭 결합·3계정 살포면 오라는 말 없이도 HIGH (`score.ts:301`).
- [GAP] `cta` (`explain.ts:181-186`) 설명은 "연락채널 신호나 사칭 구조가 있을 때만" 으로 맞는데, 같은 항목의 라벨 (`score.ts:219`) 은 "연락채널 + 행동 유도 문구". 라벨과 설명이 서로 다른 말을 한다.
- [GAP] `impersonate` 범주·`impersonate:institution+credential` (`explain.ts:82,165`): "실제로는 해외에서 만든 계정", "최근 실제 수집 데이터에서 확인된 사칭형의 정확한 구조" — 첫 실수집에서는 사칭형 0건(`reports/2026-09-20-first-collection.md` "사칭·살포형은 0건"). 설명이 근거 없는 사실 주장을 한다. 소현이 직접 본 것이지 이 시스템이 수집한 것이 아니다.
- [RISK] `obfuscation` (`explain.ts:177`) "유인글 표본의 절반 이상에서 발견됩니다" — PIP 논문 59% 를 출처 없이 인용. `TELEGRAM` (`explain.ts:95`) "투자 리딩방의 압도적 다수가 텔레그램" — 출처 없음. 일반인 대상 설명으로는 괜찮지만 심사에서 "근거는?" 에 답이 없다.
- [OK] `spread`·`shared-contact`·`gate:no-contact`·`veto:victim`·`llm:*` 는 코드 동작(임계값·점수·상한)과 일치함을 확인.

## (d) 명예훼손·개인정보 노출 지점

- [RISK] **조회 API 전부 무인증·무제한 공개.** `src/lib/auth.ts:4` "조회 API 는 공개". `/api/accounts/:q` (`accounts/[id]/route.ts:16-40`) 는 실계정의 핸들·표시명·bio·외부링크·팔로워·**전문 본문·게시물 URL·프로필 캡처 URL·기계 점수** 를 누구에게나 준다. 레이트리밋은 `/api/feedback` 에만 (`feedback/route.ts:13-31`).
- [RISK] **리포트 페이지·JSON 이 사람 검토 전 계정에도 공개 생성된다.** `report.ts:45` 는 라벨을 안 본다. `/report/threads:<실핸들>` 은 "피신고 계정" 제목 + LLM/템플릿 "범죄사실 요약"(`llm.ts:113-122`) + 신고처 링크를 붙여 준다. CLEARED 일 때만 경고 배너 (`report/[id]/page.tsx:38`). HIGH 미검토·REVIEW·LOW 는 아무 표시 없이 신고서 형식 그대로. 명예훼손 시 "자동 분석" 면책 문구(`report.ts:96`) 한 줄뿐.
- [RISK] **`/clusters` 와 `/api/clusters` 가 실계정 id 를 나열한다.** `clusters/page.tsx:31` 이 클러스터별 `threads:<핸들>` 을 리포트 링크로 렌더. `canonicalText` 500자(`pipeline.ts:236`)도 원문 노출. 30분 수집 루프(`collector/run-collect.sh:32` → `push.ts` → `/api/ingest`)가 돌면 실계정이 여기 계속 쌓인다.
- [RISK] **`/api/entities/:id` 는 전화번호·텔레그램 id 로 계정을 역조회.** `entities/[id]/route.ts:20-40` 인증 없음, 200계정·50게시물까지. 실수집 데이터에 실제 개인 전화번호(`entities.ts:113-114` phone 규칙)가 섞이면 번호 → 계정 → 본문 역추적이 공개 API 로 된다.
- [RISK] **캡처가 공개 Blob.** `pipeline.ts:60` `access: "public"`, `addRandomSuffix: false` → URL 이 `shots/profile/threads_<핸들>.png` 로 추측 가능. 실계정 프로필 사진(얼굴)이 인증 없이 열린다.
- [RISK] 검색엔진 색인 차단 없음. `layout.tsx` 에 `robots` 메타 없음, `robots.txt` 없음 → 실핸들 리포트 페이지가 구글에 "피신고 계정 @핸들" 로 잡힐 수 있다.
- [OK] `reports/2026-09-20-first-collection.md` 는 핸들 앞 3자만 남기고 가렸다. `collector/inbox/*`·`done/*` 는 `.gitignore`. 평가셋은 `syn_*` 합성, 데모는 `sample_*`/`demo_*` 합성 (`collector/fixtures/`).
- [OK] 라이브 DB 가 지금 500 이므로 이 순간 실데이터 노출은 없다. 다만 `com.scamradar.collect.plist` 로 launchd 등록돼 있으면 DB 복구 즉시 실계정이 공개 API 로 올라간다 — 심사 기간 중 수집 루프를 끌지 판단 필요.
- [RISK] 단일 `ADMIN_TOKEN` 공유, `reviewedBy: "admin"` 고정 (`admin/decision/route.ts:42`). "사기 확인" 클릭의 책임 주체가 기록에 남지 않는다. README 가 이미 인정.

## (e) 20분 안에 되는 수정 3개 (우선순위 순)

1. **라이브 DB 500 복구 또는 홈에 명시적 안내.** Vercel 환경변수 `POSTGRES_URL` 확인 → 로컬에서 `POSTGRES_URL=<Neon URL> npm run db:push` → `API_BASE=… npm run seed`. 안 되면 `src/app/page.tsx:41` 조건을 `!hasDb || !stats` 로 바꿔 "계정 조회 일시 비활성 — 텍스트 분석은 가능" 배너가 뜨게 한다 (한 줄). 심사위원이 조용한 500 을 보는 것이 최악.
2. **판단 어휘 5곳 치환 + 색인 차단.** `layout.tsx:6` title → "리딩방 레이더 — SNS 유인 신호 조회"; `report/[id]/page.tsx:49` "1. 피신고 계정" → "1. 대상 계정", `:69` "3. 내용 (범죄사실 요약 — 요건사실 위주)" → "3. 내용 요약 (관측 사실)"; `page.tsx:57` "범죄사실요약" → "내용 요약"; `Lookup.tsx:88` "신고용 리포트 보기" → "리포트 보기"; `clusters/page.tsx:17` 마지막 문장 삭제. 같은 편집에서 `layout.tsx` metadata 에 `robots: { index: false }` 추가. 전부 문자열이라 빌드 위험 0.
3. **LLM 프롬프트에 사칭 축 추가 + CTA 라벨 교정 + t.em 오타.** `llm.ts:48` system 에 "(c) 실존 기관명 + 재직·사원증·급여명세 주장 + 정보 제공 제안이면 메신저 유도가 없어도 scam" 한 절 추가; `score.ts:219` 라벨 → "행동 유도 문구 (연락채널·사칭 구조 동반)"; `entities.ts:98` `(?:me|dog)` → `(?:me|em|dog)` (첫 실수집에서 `t.em` 이 `telegram:t.em` 으로 잡힌 건). 셋 다 한 줄. `npm test` 로 회귀 확인.

시간이 남으면: `explain.ts` 에 `"veto:타로·점술 맥락 (투자 리딩 아님)"` 전용 항목 4줄, `explain.ts:155` 문장을 "연락채널·사칭 결합·다계정 살포 중 하나가 있어야" 로.
