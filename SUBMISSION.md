# 원티드 AI Championship 2026 — 제출 폼 초안

> 폼에 붙여넣을 텍스트. 칸 이름은 폼 실물에 맞춰 조정. 초안이며 소현 검토 후 확정.

## 제목

소셜미디어 리딩방 사기 계정 탐지 및 금융기관 조회 봇

## 해결하고자 한 문제

Threads·인스타그램에는 해외에서 만든 계정이 한국인 행세를 하며 대신증권·삼성전자 같은 국내 대기업 재직자를 사칭하는 글이 매일 올라온다. 위조한 사원증과 급여명세서 사진을 신뢰 장치로 쓰고, 거의 같은 본문을 수십 개의 일회용 계정으로 뿌린 뒤 텔레그램·오픈채팅으로 사람을 끌어간다. 피해자가 신고해 계정이 지워질 때쯤이면 같은 문구가 새 계정으로 이미 다시 올라와 있다.

플랫폼은 개별 신고에 하나씩 반응하고, 금융기관과 수사기관은 SNS 계정을 볼 수단이 없다. 그 사이에 "이 계정이 그 패턴인가"를 근거와 함께 즉시 말해주는 도구가 없다. 우리는 그 구간 — 탐지 → 근거 정리 → 신고 준비 — 만 만든다. 최종 판단과 신고는 사람이 한다.

## 접근 방법

1. **수집은 브라우저, 판정은 코드.** AI 브라우저(Aside)가 로그인 세션으로 Threads를 사람처럼 검색해 게시물·프로필·캡처만 뽑는다. 판정 로직은 전부 순수 TypeScript라 재현·검증·튜닝이 되고, 브라우저에서 그대로 돌아 서버 비용이 0이다(`/analyze`).
2. **난독화 복원.** 초성(ㅌㄹㄱㄹ), 혼동문자(Ｔеlеgrаm), 제로폭 문자, 구분자 삽입(텔.레.그.램)을 되돌린 뒤에 매칭한다. 사기 계정은 자기 연락처를 난독화하고 정상 사용자는 하지 않는다 — 난독화 자체가 신호다.
3. **네 가지 신호를 묻는다.** ① 살포 — 같은 본문이 서로 다른 계정 몇 개에서 나오는가(MinHash/LSH, 문자 3-gram). ② 사칭 — 기관명 + 재직·사원증·급여명세 주장. ③ 유도 — 텔레그램·오픈채팅·DM으로 오라는 문구와 실제 연락처 값. ④ 계정 — 신규·저팔로워·외부 링크가 메신저. 문구는 바꿔도 연락처는 못 바꾸므로 같은 텔레그램 주소를 쓰는 계정을 묶어 조직 단위로 본다.
4. **AI는 경계 구간과 요약에만.** 룰 점수 40~69점 구간만 LLM이 재판정하고, 신고서의 범죄사실요약 초안을 쓴다. 내용 해시로 캐시해 같은 문구 재수집은 0원.
5. **판단 단어를 화면에 쓰지 않는다.** "의심·주의" 같은 판정 대신 점수와 신호 개수만 표시하고, 각 근거를 무엇이 감지됐고 왜 문제이며 정상적인 경우는 어떻게 다른지 예시와 함께 길게 설명한다. 사람이 "사기 확인"을 누르기 전에는 사기라는 단어가 나오지 않는다.

## 결과

- 합성 평가셋(사칭·살포 유형 포함)에서 HIGH 정밀도·재현율은 `eval/RESULTS.md`에 재현 가능한 명령과 함께 기록.
- 2026-09-20 Threads 실수집 22개 검색어·250건을 채점해 첫 실전 결과를 `reports/2026-09-20-first-collection.md`로 남김. 사전 위주 판정의 오탐(타로 "리딩" + 카카오채널)과 미탐(텔레그램 무료방 유인글이 경계 점수)을 확인했고, 그 결과로 판정 축을 "연락처 유도"에서 "사칭 + 살포"로 재설계했다.
- 계정 조회 `/`, 텍스트 분석 `/analyze`, 캠페인 묶음 `/clusters`, 신고용 리포트 `/report/:id`, 사람 검토 `/review`, 금융기관·플랫폼 봇이 붙일 수 있는 REST API(`/api/accounts`, `/api/analyze`, `/api/reports`).

## 링크

- GitHub: https://github.com/Sophia-Hong/scam-radar
- 데모: https://scam-radar-sooty.vercel.app
- 판정 정확도: https://github.com/Sophia-Hong/scam-radar/blob/main/eval/RESULTS.md
- 첫 실수집 리포트: https://github.com/Sophia-Hong/scam-radar/blob/main/reports/2026-09-20-first-collection.md
